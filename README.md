
  # Revise UI Design Layout

  This is a code bundle for Revise UI Design Layout. The original project is available at https://www.figma.com/design/4hl4DdrIomTeDb49LYYlE5/Revise-UI-Design-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  